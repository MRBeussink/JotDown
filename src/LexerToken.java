/* This enum contains tokens for the lexer
 */

public enum LexerToken {
	
	H1, H2, H3, H4, H5, H6,		// headers
	HR,							// horizontal rule
	ITAL, BOLD, CODE, STRIKE,	// emphasis
	OL, IL,						// lists
	C_BLOCK, Q_BLOCK			// blocks
}
